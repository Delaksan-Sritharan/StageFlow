import { PageLoading } from "@/components/PageLoading";

export default function Loading() {
  return (
    <PageLoading
      eyebrow="StageFlow / Invitation"
      title="Loading invitation"
      description="Preparing your join flow and session details."
    />
  );
}
