import { PageLoading } from "@/components/PageLoading";

export default function Loading() {
  return (
    <PageLoading
      eyebrow="StageFlow / Timer"
      title="Loading timer"
      description="Preparing the reusable timer workspace and visual thresholds."
    />
  );
}
