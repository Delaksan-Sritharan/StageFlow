import { PageLoading } from "@/components/PageLoading";

export default function Loading() {
  return (
    <PageLoading
      eyebrow="StageFlow / Summary"
      title="Loading summary"
      description="Collecting speaker scores and comments for review."
    />
  );
}