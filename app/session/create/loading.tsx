import { PageLoading } from "@/components/PageLoading";

export default function Loading() {
  return (
    <PageLoading
      eyebrow="StageFlow / Sessions"
      title="Loading session form"
      description="Preparing the session creation workflow."
    />
  );
}