import { PageLoading } from "@/components/PageLoading";

export default function Loading() {
  return (
    <PageLoading
      eyebrow="StageFlow / Login"
      title="Loading login"
      description="Preparing the authentication flow."
    />
  );
}
